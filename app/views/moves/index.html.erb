<h1>Fighting Game Movelist Index</h1>
<% @moves.each_with_index do |move, index| %>
<div id="move-<%= index %>">
<b><%= move.name %> - <%= move.fighter.name %></b><br>
<p>Grade: <%= move.grade %></p><br>
</div>
<% end %>